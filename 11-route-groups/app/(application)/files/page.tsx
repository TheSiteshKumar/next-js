// app/(application)/files/page.tsx
import React from 'react'

export default function FilesPage() {
  return (
    <div>
      <h2>Files</h2>
      <p>This is the files page using the application layout but the URL is just /files.</p>
      <p>In this section, users can access various file resources.</p>
    </div>
  )
}